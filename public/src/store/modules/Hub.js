const state = {
  version: '0.1'
}

const getters = {
  getHubVersion (state) {
    return state.version
  }
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
